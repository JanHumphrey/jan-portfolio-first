import React from "react";
import Image from "next/image";
import Link from "next/link";

interface BlogCardsProps {
  imageURL: string;
  title: string;
  description: string;
  url?: string;
}

export const BlogCards = (props: BlogCardsProps) => {
  return 
};
export default BlogCards;
